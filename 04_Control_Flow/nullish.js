
// With Name Usage

let name = "Ahmad"
let defaultName = "Guest";

// Use Nullish concesling operater which runs on right side if left is null or undefined

let nameToDisplay = name ?? defaultName;
console.log(nameToDisplay);


//Without Name Usage

//let name;
//let defaultName = "Guest";

// Use Nullish concesling operater which runs on right side if left is null or undefined

//let nameToDisplay = name ?? defaultName;
//console.log(nameToDisplay);
